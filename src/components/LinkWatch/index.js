import styled from "styled-components"


const LinkWatch = styled.a `
    font-weight: bold;
    color: var(--color-white);
    cursor: pointer;
    text-decoration: none;

`


export default LinkWatch;